<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rads.com.br.drunkenmaster">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="br.com.rads.drunkenmaster.home.HomeActivity"
            android:label="@string/activity_title_delivery_address"
            android:launchMode="singleTop"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="br.com.rads.drunkenmaster.product.list.ProductListActivity"
            android:launchMode="singleTop"
            android:parentActivityName="br.com.rads.drunkenmaster.home.HomeActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name="br.com.rads.drunkenmaster.product.detail.ProductDetailActivity"
            android:label="@string/activity_title_product_detail"
            android:parentActivityName="br.com.rads.drunkenmaster.product.list.ProductListActivity"
            android:screenOrientation="portrait" />

        <service
            android:name="br.com.rads.drunkenmaster.geocode.FetchAddressIntentService"
            android:exported="false" />

    </application>

</manifest>